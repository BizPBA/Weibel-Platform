// Passkey functionality is currently disabled.
// The original implementation is preserved in version control and can be
// restored when the feature is ready to be reactivated.
//
// To re-enable:
//   1. Restore the original PasskeyAuth component from git history.
//   2. Re-enable imports and usages in LoginForm.tsx and Settings.tsx.
//   3. Restore src/lib/passkey.ts helpers.

export {}
